# ESP-IDF build output and generated configuration. sdkconfig can contain
# per-device provisioning and API credentials and must never be committed.
build/
managed_components/
sdkconfig
sdkconfig.old
sdkconfig.local
secrets/
*.bin
*.elf
*.map
